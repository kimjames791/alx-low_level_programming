#include "stdio.h"

/**
 * _islower - check for lower case letter in the func
 * @c : character to check the case
 * Return: 0 or 1 if success
 */

int _islower(int c)
{
	return (c >= 97 && c <= 122);

}
