#include "main.h"
/**
 * mulc -finction that checks for a digit from 0
 * @a: a is avariable
 * @b: b is a variable
 * Return: always 0.
 */

int mul(int a, int b);
{
	int mul;

	mul = a * b;
	return (mul);
}
