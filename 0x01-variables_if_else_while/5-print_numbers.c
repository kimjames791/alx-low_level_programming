#include <stdio.h>
/**
 *  main - this is the main entry point
 *  Return: always return 0 on success
 */

int main(void)
{
	int d;

	for (d = 0; d <= 10; d++)
	printf("%d", d);
	putchar('\n');
	return (0);
}

