#include "main.h"

/**
 * _sqrt_recursion - finds the natural square
 * @n: the number to find
 * @i: iterate number
 * Return: the natural sqr
 */
int _sqrt_recursion(int n)
{
	int sqr = i * i;

	if (sqr > n)
		return (-1);
	else if (sqr == n)
		return (i);
	else
		return (_sqr(n, 1 + 1))
}
