#include "main.h"
#include <stdio.h>

/**
 * main - prints program name fillowed by anew line
 * @argc: number of arguments passed
 * @argv: array of arguments
 *
 * Return: return 0
 */

int main(int argc, char const *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
